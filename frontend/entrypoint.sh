#!/bin/sh
# =============================================================================
# Domino Frontend — 启动脚本（无运行时注入）
# 构建时已通过 Vite 的 DefinePlugin 注入环境变量
# =============================================================================

echo "[domino-frontend] Starting nginx..."
exec nginx -g "daemon off;"
