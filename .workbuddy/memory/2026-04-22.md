# 2026-04-22 工作日志

## Domino 优化任务（第二阶段）— 前端 + 镜像 + 部署

### 前端优化

- `frontend/src/context/workspaces/types/workspaces.ts`
  - `repositorySource` enum 新增 gitlab/gitea/bitbucket/local
  - `WorkspaceSummary` 新增 `git_access_token_filled`, `git_platform_type`, `git_platform_url`, `git_username`
  - 保留 `github_access_token_filled` 向后兼容

- `frontend/src/features/workspaces/api/workspace/useUpdateWorkspace.ts`
  - `PatchWorkspaceParams` 新增 `git_access_token`, `git_platform_url`, `git_platform_type`, `git_username`

- `frontend/src/features/workspaces/components/workspaceSettings/WorkspaceSecretsCard.tsx`
  - 完全重写：从单一 GitHub token 表单 → 通用 Git 平台配置卡片
  - 新增平台类型下拉（GitHub/GitLab/Gitea/Bitbucket）
  - 新增平台 URL 字段（自托管实例）
  - 新增用户名字段
  - 使用 react-hook-form Controller 管理 Select

- `frontend/src/features/workspaces/components/workspaceSettings/RepositoriesCard.tsx`
  - 仓库 URL 解析逻辑重写：支持 github.com/gitlab.com/bitbucket.org 及任意自托管
  - 输入框 helperText 更新为多平台提示
  - 仓库列表图标按平台显示不同图标（GitHub/StorageIcon/FolderIcon）

- `frontend/package.json`
  - node 版本限制从 `>=18 <20` 改为 `>=18`
  - 所有依赖升级至最新稳定版（axios 1.7.7, vite 5.4.10, react-toastify 10.0.5, etc.）

### imageBuild/ 目录（新建）

```
imageBuild/
├── airflow/   Dockerfile.prod (.dev)   — apache/airflow:2.9.3-python3.11
├── rest/      Dockerfile.prod (.dev)   — python:3.11-slim + 非root用户
├── frontend/  Dockerfile.prod (.dev)   — 多阶段 node:20 → nginx:1.27
├── piece/     Dockerfile.prod (.dev) .gpu-prod .gpu-dev — python:3.11-slim / cuda:12.3.1
└── README.md  — 详细构建说明文档
```

安全改进：CUDA 从 11.8 升级至 12.3.1，所有 Dockerfile 使用 --no-install-recommends，rest 生产镜像使用非 root 用户

### deploy/ 目录（新建）

```
deploy/
├── docker/
│   ├── compose.yaml        — 完整生产 compose（含 domino + airflow 全服务）
│   └── .env.example        — 环境变量模板
├── k8s/
│   └── README.md           — Helm 部署文档
└── README.md               — 部署方式选择指南
```

compose.yaml 主要改进：
- 使用无 version 字段的新 Compose 规范
- postgres 升级至 16-alpine，redis 升级至 7-alpine
- airflow-postgres 与 domino-postgres 完全分离
- 所有服务使用 `unless-stopped` 重启策略
- 所有密码通过 `.env` 注入，支持 `:?` 强制检查
- airflow-init 使用新版 `_AIRFLOW_DB_MIGRATE` 变量

### 第一阶段（昨日/上次）已完成的后端修改
见之前的会话记录（rest/ 各文件修改）

## 前端镜像构建修复

### 第二轮 tsc 编译错误（6 个）
- `validationResolver.ts` — ValidationError 改为值导入（非 type-only）；返回值 as any 绕过 Resolver 严格类型
- `PieceFormDrawer/index.tsx` — useForm<any> 兼容 reset(data)；data.inputs 索引访问 as any
- `WorkflowEditor/index.tsx` — resolver 直接调用需 3 参数（v7.53），加 as any 绕过

### 第一轮 tsc 编译错误（10 个）
- `DownloadPDF/index.tsx` — react-to-print v3 content → contentRef + useRef
- `RenderPDF/index.tsx` — pdfjs-dist 多版本冲突，onLoadSuccess as any
- `TaskLogs.tsx` + `RunNode/index.tsx` + `DefaultNode/index.tsx` — CSSProperties → SxProps<Theme>
- `react-query.client.ts` — UseInfiniteQueryOptions 6 泛型参数减为 5
- `validationResolver.ts` — 添加泛型 <TFieldValues>，返回 Resolver<TFieldValues>

### 依赖版本修复
- `package.json` — @mui/x-date-pickers ^6.20.4 → ^6.20.2（6.20.4 不存在）
- `Dockerfile.prod` — --frozen-lockfile → --no-frozen-lockfile
- 如需恢复严格锁定：在 frontend/ 目录本地执行 `pnpm install` 生成新 pnpm-lock.yaml
