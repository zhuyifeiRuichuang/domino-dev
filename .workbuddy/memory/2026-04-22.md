# 2026-04-22 Domino 部署模式重构

## 主要变更

### 1. 简化部署模式枚举
- 文件: `src/domino/schemas/deploy_mode.py`
- 变更: 简化 DeployModeType 枚举为三种标准模式
  - `host`: 主机环境部署（直接在宿主机运行 Piece）
  - `docker-compose`: Docker Compose 部署
  - `k8s`: Kubernetes 部署

### 2. 删除代码中的多余部署模式
- 删除 `local-k8s-dev`, `local-k8s`, `local-compose`, `local-bash`, `local-python` 等旧模式
- 相关文件:
  - `src/domino/cli/cli.py` - 删除 `--deploy-mode` 及相关参数
  - `src/domino/cli/utils/platform.py` - 简化 `prepare_platform` 和 `create_platform`
  - `src/domino/base_piece.py` - 简化 `push_xcom` 逻辑
  - `src/domino/custom_operators/k8s_operator.py` - 删除 `local-k8s-dev` 逻辑
  - `src/domino/task.py` - 更新 `_set_operator` 使用新部署模式

### 3. 调整 Docker Compose 部署
- 文件: `deploy/docker/compose.yaml`
- 变更:
  - 统一使用 `context: ../..` (项目根目录)
  - 修复 Dockerfile 路径为相对路径
  - 添加 `--build` 参数支持自动构建镜像
  - 简化 DOMINO_DEPLOY_MODE 为固定值 `docker-compose`

### 4. 更新文档
- `imageBuild/README.md` - 添加三种部署方式说明
- `deploy/README.md` - 更新部署方式选择
- `deploy/k8s/README.md` - 更新部署模式说明
- `deploy/docker/.env.example` - 简化部署模式配置

### 5. 配置文件清理
- `src/domino/cli/utils/config-domino-local.toml` - 删除 `[dev]` 配置段

## 部署方式
1. 主机环境: 直接运行脚本（非容器化）
2. Docker Compose: `cd deploy/docker && docker compose up -d`
3. Kubernetes: `helm install domino ./helm/domino`

## 修复: Docker 构建错误 (requirements.txt not found)

**问题**: compose.yaml 中构建上下文改为项目根目录 `context: ../..`，但 Dockerfile 中的 `COPY requirements.txt .` 期望文件在构建上下文根目录。

**修复**:
- `imageBuild/rest/Dockerfile.prod`: 
  - `COPY requirements.txt .` → `COPY rest/requirements.txt .`
  - `COPY . .` → `COPY rest/ .`
- `imageBuild/frontend/Dockerfile.prod`:
  - 所有 COPY 路径添加 `frontend/` 前缀
  - 包括 package.json、src/、public/、scripts/ 等

